{
  "name": "downside",
  "version": "0.1.0",
  "private": true,
  "description": "Probabilistic outcome engine for cross-border education debt. Returns a distribution, not a number.",
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "sim": "node src/cli.ts",
    "test": "node --test \"test/*.test.ts\"",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "node scripts/build-site.mjs",
    "runway": "node src/runway.ts"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.6.3"
  }
}
